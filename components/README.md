# ⚠️ Archived — Historical Reference Only

This documentation refers to the **old `lz-dial` component** (`LawnCzarDial` / `<lz-dial>`), which has been superseded by `bzr-dial-menu`.

**Current component:** See `/root/bzr-dial-menu/src/bzr-dial-menu.js` and `/root/bzr-dial-menu/README.md`

**API mapping (old → new):**
- `<lz-dial>` → `<bzr-dial-menu>`
- `<lz-item>` → `<bzr-item>`
- `lz-change` event → `bzr-change` event
- `lz-dial.js` → `bzr-dial-menu.js`
- `LawnCzarDial` class → `BzrDialMenu` class

The old source is archived at `/root/czarui/historical/lz-dial.js.archived`.
